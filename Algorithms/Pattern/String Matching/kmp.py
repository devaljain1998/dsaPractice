def getLPSArray(pat):
    lps_array = []

    for i in range(len(pat)):
        lps_array.append(0)

    i = 0
    
    while(i<len(pat)):
        if()

def KMPAlgo(text, pat):
    lps_array = getLPSArray(pat)

text = "H"
pat = "M"
print('Working 1')
